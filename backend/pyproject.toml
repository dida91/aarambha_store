[tool.black]
line-length = 88
target-version = ['py312']
exclude = '(migrations|\.venv|node_modules)'

[tool.ruff]
line-length = 88
target-version = 'py312'
extend-exclude = ['**/migrations/*.py', '.venv', 'node_modules']

[tool.ruff.lint]
select = ['E', 'F', 'I', 'B', 'UP']
ignore = ['E501']
